(ns madek.api.resources.users.columns)

(def user-select-keys
  [:users.accepted_usage_terms_id
   :users.created_at
   :users.email
   :users.first_name
   :users.id
   :users.institution
   :users.institutional_id
   :users.last_name
   :users.last_signed_in_at
   :users.login
   :users.notes
   :users.password_sign_in_enabled
   :users.person_id
   :users.settings
   :users.updated_at])
